{
  "name": "RPS",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "npm run build && DEV_SERVER_PORT=4040 DEV_SERVER_ROOT=build dev-server",
    "dev": "DEV_SERVER_PORT=4040 DEV_SERVER_ROOT=build dev-server",
    "build": "mkdir -p ./build/js && cp -r ./src/js ./build/ && cp index.html ./build/index.html && npm run compileSass && npm run optimizeSVG",
    "compileSass": "sass ./src/scss/app.scss ./build/css/main.css --style=expanded --embed-sources",
    "clean": "rm -r ./build && mkdir -p ./build/js && cp -r ./src/js ./build/",
    "optimizeSVG": "svgo -f ./src/img/ -o ./build/img"
  },
  "author": "SCF",
  "license": "MIT",
  "devDependencies": {
    "dev-server": "^0.2.0",
    "sass": "^1.26.3",
    "svgo": "^2.3.0"
  }
}
